{
  "name": "h5demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:login": "BUILD_PAGES=login vite build",
    "build:user-center": "BUILD_PAGES=userCenter vite build",
    "build:login-only": "BUILD_PAGES=login vite build",
    "build:user-center-only": "BUILD_PAGES=userCenter vite build",
    "build:all": "vite build",
    "build:smart": "node scripts/build.js",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.17",
    "vue-i18n": "^10.0.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^5.4.19"
  }
}
